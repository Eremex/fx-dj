#ifndef _MAIN_
#define _MAIN_

//
// Since every module must contain interface header, it is required even when
// the module does not provide any external interfaces.
//
FX_METADATA(({ interface: [MAIN, VERSION1] }))

#endif
